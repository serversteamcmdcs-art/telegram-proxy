services:
  - type: web
    name: telegram-proxy
    runtime: node
    buildCommand: echo "no build needed"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
